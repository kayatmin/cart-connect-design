import fs from 'node:fs';
import path from 'node:path';

const repoRoot = path.resolve(import.meta.dirname, '..');
const designPath = path.join(repoRoot, 'design.md');
const brandPath = path.join(repoRoot, 'brand.md');
const outDir = path.join(repoRoot, 'generated');

const design = fs.readFileSync(designPath, 'utf8').trim();
const brand = fs.readFileSync(brandPath, 'utf8').trim();

const screenPrompts = {
  splash: `Design the CartConnect mobile splash / bootstrap screen.\n\nUse this source of truth:\n${design}\n\nBrand notes:\n${brand}\n\nRequirements:\n- mobile-first startup screen for the agent app\n- CartConnect wordmark or compact identity mark\n- small operational status line such as "Syncing agent workspace"\n- calm command-center feel\n- no marketing copy, no decorative hero, no stock imagery\n- production-ready Flutter-friendly layout`,
  login: `Design a premium mobile agent login screen for CartConnect.\n\nUse this source of truth:\n${design}\n\nBrand notes:\n${brand}\n\nRequirements:\n- mobile-first\n- polished production-quality UI\n- title/brand presence\n- email and password fields\n- strong sign-in CTA\n- subtle trust-building styling\n- suitable for a live support agent tool`,
  dashboard: `Design the CartConnect mobile agent dashboard.\n\nUse this source of truth:\n${design}\n\nBrand notes:\n${brand}\n\nRequirements:\n- agent summary header\n- waiting/active/completed metrics\n- session list optimized for quick scanning\n- strong visual emphasis on waiting sessions\n- clean mobile hierarchy\n- polished, premium operational UI`,
  sessionDetail: `Design the CartConnect mobile session detail screen for agents.\n\nUse this source of truth:\n${design}\n\nBrand notes:\n${brand}\n\nRequirements:\n- customer identity\n- session status\n- accept / decline / end actions\n- chat timeline\n- cart panel with quantity controls and subtotal\n- room for checkout and discount actions\n- mobile-first and production-ready`,
  chatFocus: `Design the CartConnect mobile chat-focused session state for agents.\n\nUse this source of truth:\n${design}\n\nBrand notes:\n${brand}\n\nRequirements:\n- active customer session header with clear live state\n- chat timeline as the primary content\n- visually distinct customer vs agent messages\n- reply composer anchored predictably at the bottom\n- obvious send action with large mobile touch target\n- compact customer/cart context without crowding the conversation\n- mobile-first and production-ready`,
  cartEdit: `Design the CartConnect mobile cart edit state for agents.\n\nUse this source of truth:\n${design}\n\nBrand notes:\n${brand}\n\nRequirements:\n- active session context and customer identity\n- cart contents as the primary content\n- quantity controls with large mobile touch targets\n- remove item action that feels intentional, not alarming\n- clear subtotal and checkout readiness state\n- room for future discount and checkout actions without assuming unavailable backend features\n- mobile-first and production-ready`,
  inventoryAdd: `Design the CartConnect mobile inventory add sheet for agents.\n\nUse this source of truth:\n${design}\n\nBrand notes:\n${brand}\n\nRequirements:\n- bottom sheet or full-height modal suitable for Flutter mobile\n- search field as the first control\n- product results with thumbnail, title, variant, availability, price, and add action\n- quantity stepper with stable layout and large touch targets\n- loading and empty-search states that are quiet and useful\n- should feel connected to the session detail and cart edit screens\n- mobile-first and production-ready`,
  fullFlow: `Design a coherent mobile UI system for the CartConnect agent app.\n\nUse this source of truth:\n${design}\n\nBrand notes:\n${brand}\n\nGenerate a consistent set of screens for:\n- login\n- dashboard\n- session detail\n- chat-focused state\n- cart-edit state\n\nThe result should feel like one polished mobile product.`,
  authExpired: `Design the CartConnect mobile "Auth Expired" re-auth screen.\n\nUse this source of truth:\n${design}\n\nBrand notes:\n${brand}\n\nRequirements:\n- shown when backend returns 401 during an active agent session\n- explains session expired calmly — not alarming, just informational\n- single clear "Sign in again" CTA, no distractions\n- maintains brand consistency with the Login screen (same color family, typography, spacing)\n- minimal, single-focus layout — agent has to re-auth, no other decisions\n- no photography, no stock imagery, no large decorative hero image\n- use only simple iconography, tonal surfaces, and concise text\n- mobile-first, large touch target on the CTA\n- premium operational UI feel — not a generic error page\n- quiet background, confident copy`,
  emptyState: `Design the CartConnect mobile "No Sessions" empty state screen.\n\nUse this source of truth:\n${design}\n\nBrand notes:\n${brand}\n\nRequirements:\n- shown when agent dashboard loads with zero sessions in queue\n- reassures the agent — this is a good state, not an error or problem\n- subtle illustration or icon (professional, not playful or cartoonish)\n- brief reassuring copy — "All clear", "No waiting sessions", or similar\n- soft green or neutral tint to communicate "all clear / nothing to do"\n- no action required from agent — just informational\n- matches premium operational UI aesthetic of the rest of the app\n- generous whitespace, clean card layout\n- mobile-first`,
  apiError: `Design the CartConnect mobile API / Network Error screen.\n\nUse this source of truth:\n${design}\n\nBrand notes:\n${brand}\n\nRequirements:\n- shown when an API call fails (connection error, server error, or timeout)\n- briefly explains what happened — "Couldn't reach the server", "Connection lost"\n- prominent "Retry" button — recoverable error, not a dead end\n- "Contact support" fallback link for persistent failures\n- muted amber/warning tones for the error indicator (not alarming red)\n- clean card layout, single action focus\n- mobile-first, large touch targets\n- recoverable feel — the agent should feel confident pressing Retry\n- avoid technical jargon in user-facing text\n- premium operational UI — not a generic white error screen`
};

fs.mkdirSync(outDir, { recursive: true });
fs.writeFileSync(path.join(outDir, 'design-source.snapshot.md'), `${design}\n\n---\n\n${brand}\n`);
for (const [name, prompt] of Object.entries(screenPrompts)) {
  fs.writeFileSync(path.join(outDir, `${name}.prompt.txt`), `${prompt.trim()}\n`);
}

console.log(`Wrote ${Object.keys(screenPrompts).length} prompt files to ${outDir}`);
